import { EsCurrencyPipe } from './es-currency.pipe';

describe('EsCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new EsCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
